# Cloud-service credentials. Copy to .env and fill in real values.
# `.env` is gitignored; never commit it.
SACI_TURSO_URL=
SACI_TURSO_TOKEN=
# 64 hex digits for the `connection.encryption` example in
# examples/configs/turso.kdl.
SACI_TURSO_KEY=
