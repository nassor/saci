[package]
name = "saci-config"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
publish.workspace = true
description = "The SACI configuration language: KDL text into a self-describing value tree every connector, transformer and the host deserialize from."

[dependencies]
# `SaciError`/`SaciResult` only, so the base error variants are all this crate
# needs and the async runtime stays out of the graph.
saci-core  = { workspace = true }

kdl        = { workspace = true }
serde      = { workspace = true }
serde_json = { workspace = true }
