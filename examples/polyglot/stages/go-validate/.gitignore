# Regenerated by `componentize-go bindings` from crates/saci-processor/wit on every
# build; a committed copy silently desynchronises when pipeline.wit changes.
/wit_exports.go
/saci_pipeline_types/
/saci_pipeline_host_io/
/saci_pipeline_pipeline/

# Copied in by `cargo xtask polyglot` from examples/polyglot/generated/,
# which the emit command regenerates from the canonical Rust Order definition.
/export_saci_pipeline_pipeline/schema_gen.go

# Build output.
/validate-go.wasm
