# Copied in by `cargo xtask polyglot` from examples/polyglot/generated/,
# which the emit command regenerates from the canonical Rust Order definition.
/SchemaGen.cs

# Build output. The WIT bindings land in obj/ and are regenerated from
# crates/saci-processor/wit on every build, so they are never committed either.
bin/
obj/

# Publish artifact, copied to examples/polyglot/build/ by the same task.
/tier-cs.wasm
