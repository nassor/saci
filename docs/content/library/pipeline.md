+++
title = "Pipeline"
description = "A Pipeline builds a field-level DAG from SystemMeta, sorts it into parallel stages, validates fields against the schema registry, and retries per system."
template = "pipeline.html"
weight = 4
aliases = ["/pipeline/"]
+++
