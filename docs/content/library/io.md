+++
title = "Sources & Sinks"
description = "Two traits move Arrow rows in and out of a Dataset: Source pulls RecordBatches until EOF, Sink takes one write per cycle. One crate per transport, from local files and in-memory channels to PostgreSQL, Kafka, NATS, TCP, HTTP, S3 and DataFusion SQL, and one per byte format."
template = "io.html"
weight = 6
aliases = ["/io/"]
+++
