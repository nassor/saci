+++
title = "Systems"
description = "A System is a struct with a run method and a meta() that names every Arrow field it reads and writes, so the scheduler can order and parallelize it."
template = "systems.html"
weight = 3
aliases = ["/systems/"]
+++
